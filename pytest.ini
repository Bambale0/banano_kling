[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: tests requiring live provider APIs or external services
    standalone: opt-in standalone provider smoke scripts
asyncio_mode = auto
