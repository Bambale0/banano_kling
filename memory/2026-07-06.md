
## 2026-07-06 01:56 MSK - banano_kling project continuity

- User is working on `/root/bot/banano_kling`, a Telegram bot + Telegram Mini App for AI image/video generation, bonuses, referrals, gallery/feed, shop/premium, ratings, and daily retention mechanics. User usually writes in Russian and wants direct implementation plus Telegram-visible progress reports.
- Recent completed work before this flush: Mini App and text bot were expanded with real backend-backed sections: Home/Create/Gallery/Bonuses/Rating/Shop/Profile, clickable feed topics, “Работы для вас”, HD quality one-click prompt, “Создать за 30 секунд” templates, AI photoshoot prompts, inline bot actions, lucky wheel, leaderboard, and backend gallery/feed actions.
- Recent completed work: added ChatGPT tab to Mini App main bottom menu; made bottom navigation adaptive; checked generation errors; added `generation_tasks.error_message`; updated `fail_generation_task()` to persist failure reasons; fixed Kie webhook edge case where success without result URL now fails cleanly, refunds, saves error, and notifies the user; failure webhook stores `failCode/failMsg`; Mini App receives `error_message` for failed tasks.
- Verification from last completed code task: `npm run build` passed; `python -m py_compile bot/tma_api.py bot/database.py bot/main.py bot/keyboards.py bot/handlers/common.py` passed; targeted tests passed (`49 passed`); full regression `python -m pytest -q` passed (`254 passed`); `bot.service` restarted and active with `NRestarts=0`; `/health` returned 200; `/miniapp` returned 200; migration applied and `generation_tasks.error_message` exists; logs had no ERROR/Traceback/WARNING/database locked. Real paid Kie/Seedance request was intentionally not run to avoid spending production limits.
- VibeSkills install prompt appeared in a user message, but no actual `<source>`, host, or selected version was provided. It was not installed; code work was completed directly. If user asks again to install VibeSkills, first ask the two required questions from their prompt.

## 2026-07-06 02:05 MSK - Boom project alias

- User asked to record `/root/bot/banana_kling (Boom)` in the project map. The actual local project directory is `/root/bot/banano_kling`; treat `/root/bot/banana_kling` as the user's alias/typo for the Boom project unless a separate directory appears later.
