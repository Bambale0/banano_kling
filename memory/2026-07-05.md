# Memory — 5 июля 2026

## Сессия: БухCRM — реферальный аудит Таня ТГ + завершение задачи

### Что сделано
- Проведён полный аудит реферальной системы @Neuromixx_bot (Таня ТГ)
- Найдена и исправлена DRY-регрессия (`f2064dc` → `0b1dbd3`): в `miniapp.py` остались 2 вызова старого имени функции `_referral_code_from_start_param` → NameError на каждом запросе Mini App API. Фикс: переименовал вызовы в `_get_referral_code_from_init_data`.
- Пофикшен косметический баг (`167e42a`): `bonus_credits = 0` → `bonus_credits = 3` в двух INSERT в `referral_service.py`. Реально бананы начислялись (через UPDATE), но таблица `referrals` врала.
- Разобраны жалобы партнёров:
  - @only_tany (код `79C6TGJ1`): `credits=11`, `referral_earned=108` (36×3). Бананы начислялись, просто потратила.
  - @MiroslavaS_mom (код `SSFHTGDH`): 0/0 — 10+ кликов терялись из-за NameError бага (теперь фикс).
- Выявлено ограничение платформы: `?startapp=ref_CODE` не работает на Telegram Desktop (Mini App не грузится). На мобиле — работает.
- Игорь протестировал на Android — bootstrap() так и не вызвался. За этот сеанс подтверждено: за всю историю бота `POST /mini-app/api/bootstrap` не вызывался ни разу.

### Коммиты
- `0b1dbd3` — фикс DRY NameError (основной, решает проблему)
- `167e42a` — фикс bonus_credits (косметика)
- Ветка: `tanyapi`, 14 коммитов, не слито в main

### Статус на выходе
- Реферальная система работает корректно для `?start=` ссылок
- Mini App путь пофикшен для мобильных
- Desktop Mini App — платформенное ограничение
- Нужно разобраться, почему на андроиде Mini App не прогрузился

### Отложено
- Мерж tanyapi → main
- Пуш 7 тестовых файлов (143 теста) — токен GitHub протух
- stupidbot полный аудит (CRM ID 4, сервер 217.11.166.233)
- Яндекс Директ (~14:00 MSK)
