# Telegram Bot Framework
aiogram>=3.0.0

# Async HTTP Client
aiohttp>=3.8.0

# Database
aiosqlite>=0.19.0

# Google Gemini API
google-genai>=0.3.0
Pillow>=10.0.0

# Environment variables (optional, for .env support)
python-dotenv>=1.0.0

# Type hints
typing-extensions>=4.0.0

# YooKassa payments
yookassa>=3.4.0
pandas>=2.2.2
openpyxl>=3.1.2

# Optional (face detection for improved frontal crops)
# Installing face_recognition requires dlib and system build deps (cmake, boost).
# On Debian/Ubuntu you may need: apt install build-essential cmake libboost-all-dev
# Then install the package:
# pip install face_recognition
# If you cannot build dlib, skip this — the service will fall back to center-crop.
